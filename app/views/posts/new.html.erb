<h1>New Post</h1>
<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :value %>
    <%= f.text_field :value %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_with model: @user_value, local:true do |f| %>
        <!--<div class="row mb-3">-->
        <!--  <%#= f.label :post_id, "グラフ項目", class: "col-md-4" %>-->
        <!--  <%#= f.select :post_id, Post.get_post_list.unshift("-- 選択して下さい --"), {}, {class: 'form-control-sm col-md-8'}%>-->
        <!--</div>-->
        <div class="row mb-3">
          <%= f.label :title, "グラフ項目", class: "col-md-4" %>
          <%= f.text_field :title, class: 'form-control price col-md-8' %>
        </div>
        <div class="row mb-3">
          <%= f.label :value, "値", class: "col-md-4" %>
          <%= f.text_field :value, class: 'form-control price col-md-8' %>
        </div>
         <div class="row my-5">
          <div class="col-5 offset-3 text-center"><%= f.submit class: 'py-3 px-5 btn btn-success' %></div>
        </div>
<% end %>


<%= link_to 'Back', posts_path %>
